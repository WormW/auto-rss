## 改动点
- 后端 `qB state -> DB status` 映射收敛：仅 `downloading` / `forcedDL` 归类为 `downloading`，`stalledDL` 及其他非活跃 DL 态（`pausedDL` / `queuedDL` / `checkingDL` / `metaDL`）归类为 `stalled`。
- 保持既有语义：`uploading` / `*UP` -> `completed`，`error` / `missingFiles` -> `failed`。
- 新增状态映射单测，确保 `stalled` 与 `downloading` 不回归混淆。
- 前端 Downloads 页已支持 `stalled` 标签与筛选（当前 `main` 已包含该能力），本 PR 聚焦后端映射精确化，保证筛选结果语义一致。

## 验证命令与结果
- `go test ./...` ✅ 通过
- `cd web && npm run build` ✅ 通过（仅 Vite chunk size warning，不影响构建成功）

## 风险 / 回滚点
- 风险：部分历史上标为 `downloading` 的任务会在后续监控轮次转为 `stalled`，属于预期语义修正，可能导致短期统计口径变化。
- 回滚：revert 本 PR 的提交即可恢复原映射逻辑。
