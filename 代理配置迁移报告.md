# 代理配置迁移报告

## 变更概述

将代理配置从 RSS 配置部分移至系统设置部分，使其成为全局系统配置。

## 修改文件清单

### 前端文件

#### 1. `web/src/views/Config.vue`

**模板变更**：
- 从 "RSS 配置" 卡片移除代理地址输入框
- 在 "系统设置" 卡片添加代理地址输入框

```vue
<!-- 新增到系统设置 -->
<n-form-item label="代理地址">
  <n-input
    v-model:value="systemConfig.proxy"
    placeholder="http://127.0.0.1:7890 或 socks5://127.0.0.1:7890"
  />
</n-form-item>
```

**数据模型变更**：
```typescript
// BEFORE
const rssConfig = ref({
  interval: 30,
  downloadPath: '/downloads',
  proxy: ''  // 在这里
})

const systemConfig = ref({
  logLevel: 'info',
  autoRename: true
})

// AFTER
const rssConfig = ref({
  interval: 30,
  downloadPath: '/downloads'  // 移除 proxy
})

const systemConfig = ref({
  logLevel: 'info',
  autoRename: true,
  proxy: ''  // 移动到这里
})
```

**配置加载变更**：
```typescript
// 从 'rss_proxy' 改为 'system_proxy'
case 'system_proxy':
  systemConfig.value.proxy = config.value
  break
```

**保存函数变更**：
```typescript
// saveRSSConfig - 移除代理保存
const saveRSSConfig = async () => {
  await configApi.update('rss_interval', rssConfig.value.interval.toString())
  await configApi.update('download_path', rssConfig.value.downloadPath)
  // 移除：await configApi.update('rss_proxy', rssConfig.value.proxy)
}

// saveSystemConfig - 添加代理保存
const saveSystemConfig = async () => {
  await configApi.update('log_level', systemConfig.value.logLevel)
  await configApi.update('auto_rename', systemConfig.value.autoRename.toString())
  await configApi.update('system_proxy', systemConfig.value.proxy)  // 新增
}
```

### 后端文件

#### 2. `internal/api/handler/rss_source.go`

**配置键变更**：
```go
// BEFORE
proxyConfig, err := h.configRepo.Get("rss_proxy")

// AFTER
proxyConfig, err := h.configRepo.Get("system_proxy")
```

## 数据库配置键变更

| 原配置键 | 新配置键 | 用途 |
|---------|---------|------|
| `rss_proxy` | `system_proxy` | HTTP/SOCKS5 代理地址 |

## 用户体验变化

### 变更前
- 代理配置位于 "RSS 配置" 部分
- 仅在 RSS 设置时可见
- 配置键：`rss_proxy`

### 变更后
- 代理配置位于 "系统设置" 部分
- 作为全局系统配置更易发现
- 配置键：`system_proxy`
- 配置范围：影响所有使用代理的系统功能

## 构建结果

### 前端构建
```bash
vite v5.4.20 building for production...
✓ 2860 modules transformed.
dist/assets/index-E7qX7ahG.js   773.70 kB │ gzip: 226.22 kB
✓ built in 2.10s
```

### 后端构建
```bash
go build -o auto-rss cmd/server/main.go
✓ 构建成功
```

## 向后兼容性

如果数据库中存在旧的 `rss_proxy` 配置：
1. 不会自动迁移到 `system_proxy`
2. 用户需要在系统设置中重新配置代理
3. 建议在部署后通知用户重新设置代理配置

## 测试建议

1. **配置保存测试**：
   - 在系统设置中设置代理地址
   - 点击保存配置
   - 刷新页面验证代理配置已保存

2. **代理功能测试**：
   - 配置系统代理
   - 添加 RSS 源
   - 点击 "查看番剧" 验证代理是否生效

3. **配置迁移测试**（可选）：
   - 如有旧的 `rss_proxy` 配置，手动在系统设置重新配置

## 技术要点

### 配置分离原则
- **RSS 配置**：RSS 特定参数（检查间隔、下载路径）
- **系统设置**：全局系统参数（日志级别、自动重命名、代理）

### 代理使用范围
当前代理配置应用于：
- RSS 源数据获取
- 未来可能扩展到其他需要网络访问的功能

## 总结

成功将代理配置从 RSS 专属配置提升为系统级全局配置，提高了配置的可见性和可维护性。配置键从 `rss_proxy` 更改为 `system_proxy`，反映其作为系统全局配置的新定位。
