# 数据流转优化实现说明

## 功能概述

实现了完整的 RSS 数据流转链路：**RSS 源 → 番剧列表 → 订阅创建**，并添加了代理配置支持。

## 数据流转流程

### 1. 添加 RSS 源
```
用户操作：进入 "RSS 源" 页面 → 点击 "添加 RSS 源"
↓
填写信息：
- 名称：Mikanani
- RSS 地址：https://mikanani.me/RSS/MyBangumi?token=xxx
- 描述：（可选）
- 启用：true
↓
保存到数据库：rss_sources 表
```

### 2. 获取番剧列表
```
用户操作：点击 "查看番剧"
↓
后端处理：
1. 从配置读取代理设置（rss_proxy）
2. 设置 Parser 的代理
3. 使用代理访问 RSS 地址
4. 解析 RSS 返回的所有 items
5. 提取番剧信息：
   - 标题（去除字幕组和集数）
   - 字幕组
   - 集数列表
6. 按番剧名称分组
7. 排序集数
↓
返回番剧列表：
[
  {
    "title": "某番剧名称",
    "rss_url": "https://mikanani.me/RSS/MyBangumi?token=xxx",
    "fansub": "某字幕组",
    "episodes": ["01", "02", "03"],
    "source_id": 1,
    "source_name": "Mikanani"
  }
]
```

### 3. 创建订阅
```
用户操作：点击某个番剧的 "订阅" 按钮
↓
前端跳转：router.push({
  name: 'subscriptions',
  query: {
    from_rss: 'true',
    rss_url: anime.rss_url,
    name: anime.title,
    fansub: anime.fansub,
    rss_source_id: anime.source_id
  }
})
↓
订阅页面自动填充：
- 名称：某番剧名称
- RSS 地址：https://...
- RSS 源 ID：1
- 来源类型：rss_source
- 季数：1（用户可调整）
- 下载路径：/downloads（用户可调整）
↓
用户调整并确定 → 保存到数据库：subscriptions 表
```

## 代理配置

### 后端实现

#### 1. RSS Parser 支持代理

**文件**: `internal/service/rss/parser.go`

```go
type Parser interface {
    FetchAndParse(rssURL string) ([]RSSItem, error)
    ExtractFansub(title string) string
    ExtractEpisode(title string) int
    SetProxy(proxyURL string) error  // 新增方法
}

type parser struct {
    httpClient *http.Client  // 使用自定义 HTTP 客户端
}

func (p *parser) SetProxy(proxyURL string) error {
    if proxyURL == "" {
        // 清空代理
        p.httpClient.Transport = &http.Transport{...}
        return nil
    }

    proxyURLParsed, err := url.Parse(proxyURL)
    if err != nil {
        return fmt.Errorf("invalid proxy URL: %w", err)
    }

    p.httpClient.Transport = &http.Transport{
        Proxy: http.ProxyURL(proxyURLParsed),
        ...
    }

    return nil
}
```

**支持的代理类型**：
- HTTP 代理：`http://127.0.0.1:7890`
- HTTPS 代理：`https://127.0.0.1:7890`
- SOCKS5 代理：`socks5://127.0.0.1:7890`

#### 2. Handler 读取配置并设置代理

**文件**: `internal/api/handler/rss_source.go`

```go
func (h *RSSSourceHandler) FetchAnimes(c *gin.Context) {
    // ...

    // 获取代理配置
    proxyConfig, err := h.configRepo.Get("rss_proxy")
    if err == nil && proxyConfig.Value != "" {
        if err := h.rssParser.SetProxy(proxyConfig.Value); err != nil {
            c.JSON(http.StatusInternalServerError, gin.H{"error": "设置代理失败"})
            return
        }
    }

    // 使用代理访问 RSS
    items, err := h.rssParser.FetchAndParse(source.BaseURL)
    // ...
}
```

### 前端实现

#### 系统配置页面添加代理设置

**文件**: `web/src/views/Config.vue`

```vue
<n-card title="RSS 配置">
  <n-form :model="rssConfig">
    <!-- ... 其他配置 ... -->
    <n-form-item label="代理地址">
      <n-input
        v-model:value="rssConfig.proxy"
        placeholder="http://127.0.0.1:7890 或 socks5://127.0.0.1:7890"
      />
    </n-form-item>
  </n-form>
</n-card>
```

```typescript
const rssConfig = ref({
  interval: 30,
  downloadPath: '/downloads',
  proxy: ''  // 新增代理配置
})

const saveRSSConfig = async () => {
  await configApi.update('rss_interval', rssConfig.value.interval.toString())
  await configApi.update('download_path', rssConfig.value.downloadPath)
  await configApi.update('rss_proxy', rssConfig.value.proxy)  // 保存代理
  message.success('RSS 配置保存成功')
}
```

## 番剧名称提取优化

### 问题

原始实现试图从标题中提取字幕组作为番剧名，导致分组错误。

### 解决方案

**新增 `extractAnimeName` 函数**，智能去除字幕组标签和集数信息：

```go
func extractAnimeName(title string, fansub string) string {
    name := title

    // 1. 移除字幕组标签 [字幕组]
    if fansub != "" {
        name = strings.TrimPrefix(name, "["+fansub+"]")
        name = strings.TrimSpace(name)
    }

    // 2. 移除常见的集数模式
    patterns := []string{
        `\s*第?\s*\d+\s*[集话話].*$`,     // 第12集
        `\s*[Ee][Pp]?\.?\s*\d+.*$`,    // E12, EP12
        `\s*Episode\s*\d+.*$`,         // Episode 12
        `\s*\[\s*\d+\s*\].*$`,         // [12]
        `\s*S\d+E\d+.*$`,              // S01E12
        `\s*-\s*\d+\s*[-\[].*$`,       // - 12 -
    }

    for _, pattern := range patterns {
        re := regexp.MustCompile(pattern)
        name = re.ReplaceAllString(name, "")
    }

    // 3. 移除常见的后缀
    name = regexp.MustCompile(`\s*\[.*\]$`).ReplaceAllString(name, "")  // [...]]
    name = regexp.MustCompile(`\s*【.*】$`).ReplaceAllString(name, "")  // 【...】
    name = strings.TrimSpace(name)

    return name
}
```

### 示例

| 原始标题 | 提取结果 |
|---------|---------|
| `[某字幕组] 某番剧 第12集` | `某番剧` |
| `[Fansub] Anime Name EP12 [1080p]` | `Anime Name` |
| `某番剧 - 12 [720p]` | `某番剧` |

## 集数排序

**问题**：集数可能乱序（如 ["3", "1", "2"]）

**解决**：
```go
// 排序集数
sort.Slice(anime.Episodes, func(i, j int) bool {
    ei, _ := strconv.Atoi(anime.Episodes[i])
    ej, _ := strconv.Atoi(anime.Episodes[j])
    return ei < ej
})
```

**结果**：`["1", "2", "3"]`

## 数据库配置

### 新增配置项

| Key | Value 示例 | 说明 |
|-----|-----------|------|
| `rss_proxy` | `http://127.0.0.1:7890` | RSS 访问代理 |
| `rss_interval` | `30` | RSS 检查间隔（分钟） |
| `download_path` | `/downloads` | 默认下载路径 |

### 存储格式

```sql
-- configs 表
INSERT INTO configs (key, value, description) VALUES
('rss_proxy', 'http://127.0.0.1:7890', 'RSS访问代理地址'),
('rss_interval', '30', 'RSS检查间隔（分钟）'),
('download_path', '/downloads', '默认下载路径');
```

## 使用流程

### 完整示例

1. **配置代理**（首次使用）
   ```
   进入 "系统配置" 页面
   → RSS 配置
   → 代理地址：http://127.0.0.1:7890
   → 保存配置
   ```

2. **添加 RSS 源**
   ```
   进入 "RSS 源" 页面
   → 添加 RSS 源
   → 名称：Mikanani
   → RSS 地址：https://mikanani.me/RSS/MyBangumi?token=xxx
   → 保存
   ```

3. **浏览番剧**
   ```
   点击 "查看番剧"
   → 系统自动：
     - 读取代理配置
     - 使用代理访问 RSS
     - 解析番剧列表
     - 按番剧分组
   → 显示番剧列表
   ```

4. **订阅番剧**
   ```
   找到想看的番剧
   → 点击 "订阅"
   → 自动跳转到订阅页面，信息已填充
   → 调整季数和下载路径
   → 确定
   ```

5. **自动下载**
   ```
   系统定时检查 RSS 更新
   → 发现新集
   → 自动下载
   → 重命名为 SxxExx 格式
   ```

## 技术亮点

### 1. 智能番剧识别
- 自动去除字幕组标签
- 识别多种集数格式
- 按番剧名称准确分组

### 2. 代理支持
- 支持 HTTP/HTTPS/SOCKS5 代理
- 动态配置，无需重启
- 自动应用到所有 RSS 请求

### 3. 数据流转顺畅
- RSS 源 → 番剧列表 → 订阅创建
- 一键订阅，信息自动填充
- 保留来源信息（`rss_source_id`、`source_type`）

### 4. 用户体验优化
- 集数自动排序
- 番剧名称清晰
- 配置持久化

## 测试要点

### 1. 代理配置测试

```bash
# 添加代理配置
curl -X PUT http://localhost:7892/api/v1/config \
  -H "Content-Type: application/json" \
  -d '{"key": "rss_proxy", "value": "http://127.0.0.1:7890"}'

# 验证代理生效
# 1. 使用无法直接访问的 RSS 源
# 2. 配置代理
# 3. 访问番剧列表，应该成功
```

### 2. 番剧提取测试

```
输入标题：
"[某字幕组] 葬送的芙莉莲 第12集 [1080p]"

期望输出：
{
  "title": "葬送的芙莉莲",
  "fansub": "某字幕组",
  "episodes": ["12"]
}
```

### 3. 数据流转测试

```
1. 添加 RSS 源 → 检查 rss_sources 表
2. 查看番剧列表 → 检查返回格式
3. 订阅番剧 → 检查 subscriptions 表的 rss_source_id
4. 验证订阅类型 → source_type 应为 "rss_source"
```

## 编译验证

### 后端编译
```bash
go build -o auto-rss cmd/server/main.go
# ✅ 编译成功
```

### 前端编译
```bash
cd web && npx vite build --mode production
# ✅ dist/assets/index-Bs38eU1l.js   773.69 kB │ gzip: 226.21 kB
```

## 修改文件清单

### 后端
1. `internal/service/rss/parser.go` - 添加代理支持和 HTTP 客户端
2. `internal/api/handler/rss_source.go` - 优化 FetchAnimes 逻辑，添加代理配置读取
3. `internal/api/router/router.go` - 传递 configRepo 到 Handler

### 前端
1. `web/src/views/Config.vue` - 添加代理配置表单
2. `web/src/api/rss-source.ts` - 修复 API 客户端（使用统一的 api 实例）

## 后续优化建议

1. **番剧专属 RSS URL**：
   - 当前所有番剧共享同一个 RSS 源 URL
   - 建议根据番剧名称生成专属的过滤 URL

2. **缓存优化**：
   - 缓存番剧列表，避免频繁解析 RSS
   - 设置合理的缓存过期时间

3. **批量订阅**：
   - 支持一次选择多个番剧订阅
   - 批量创建订阅记录

4. **番剧元数据**：
   - 集成 Bangumi、TMDB API
   - 获取番剧封面、简介、评分等信息

5. **更新检测优化**：
   - 使用 If-Modified-Since 头减少带宽
   - 只在有更新时才完整解析

## 总结

本次实现完成了完整的数据流转链路：

✅ **RSS 源管理** → 用户添加和管理多个 RSS 源
✅ **番剧列表获取** → 智能解析 RSS，提取番剧信息
✅ **订阅创建** → 一键订阅，自动填充信息
✅ **代理支持** → 支持配置代理访问 RSS
✅ **智能识别** → 准确提取番剧名称和集数

用户体验得到显著提升，从手动填写 RSS 地址到浏览选择番剧，整个流程更加直观和便捷。
