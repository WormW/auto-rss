# 订阅管理语言选择功能完整实现报告

## 概述

成功实现了完整的订阅管理流程中的**字幕语言选择功能**。用户现在可以:

1. 搜索番剧名称
2. 查看多个字幕组选项
3. 展开字幕组查看详细信息
4. **选择字幕语言(简体/繁体)**
5. 订阅时保存语言偏好

## 实现清单

### ✅ 前端实现

#### 1. `web/src/components/AnimeSearch.vue` - 搜索组件更新

**新增功能**:

1. **语言选项提取** - `getLanguageOptions(tags)` 函数
   ```typescript
   const getLanguageOptions = (tags: string[]) => {
     const languages: Array<{ label: string; value: string }> = []

     // 检测简体中文
     if (tags.some(tag => tag === '简体' || tag === '简中' || tag === 'CHS')) {
       languages.push({ label: '简体中文', value: 'CHS' })
     }

     // 检测繁体中文
     if (tags.some(tag => tag === '繁体' || tag === '繁中' || tag === 'CHT')) {
       languages.push({ label: '繁體中文', value: 'CHT' })
     }

     return languages
   }
   ```

2. **语言选择状态管理**
   ```typescript
   const selectedLanguage = ref<Record<string, string>>({})

   const selectLanguage = (animeUrl: string, groupRss: string, language: string) => {
     const key = `${animeUrl}_${groupRss}`
     selectedLanguage.value[key] = language
   }
   ```

3. **UI改进**:
   - 语言选择按钮组(简体中文/繁體中文)
   - 选中状态高亮显示(primary type)
   - 未选择语言时禁用订阅按钮
   - 订阅前验证语言已选择

**UI布局**:
```vue
<!-- 语言选择区 -->
<div v-if="getLanguageOptions(group.tags).length > 0">
  <n-text>选择字幕语言:</n-text>
  <n-space>
    <n-button
      v-for="lang in getLanguageOptions(group.tags)"
      :key="lang.value"
      size="small"
      :type="selectedLanguage[key] === lang.value ? 'primary' : 'default'"
      @click="selectLanguage(anime.url, group.rss, lang.value)"
    >
      {{ lang.label }}
    </n-button>
  </n-space>
</div>

<!-- 订阅按钮 -->
<n-button
  :disabled="hasLanguageOptions && !selectedLanguage[key]"
  @click="handleSubscribe(anime, group)"
>
  订阅
</n-button>
```

**数据流更新**:
```typescript
const handleSubscribe = (anime: MikanAnimeItem, group: any) => {
  const key = `${anime.url}_${group.rss}`
  const language = selectedLanguage.value[key]

  // 验证语言选择
  const languageOptions = getLanguageOptions(group.tags || [])
  if (languageOptions.length > 0 && !language) {
    message.warning('请先选择字幕语言')
    return
  }

  emit('subscribe', {
    title: anime.title,
    rss_url: group.rss,
    fansub: group.name,
    language: language,  // 新增
    rss_source_id: undefined
  })
}
```

#### 2. `web/src/views/Subscriptions.vue` - 订阅管理页更新

**表单数据扩展**:
```typescript
const formData = ref({
  name: '',
  rss_url: '',
  fansub: '',
  language: '',        // 新增
  update_day: '',
  season: 1,
  total_episodes: 0,
  episode_offset: 0,
  download_path: '/downloads',
  filter_rules: '',
  enabled: true,
  rss_source_id: undefined,
  source_type: 'manual'
})
```

**接收搜索结果**:
```typescript
const handleSearchSubscribe = (data: {
  title: string
  rss_url: string
  fansub: string
  language?: string    // 新增
  rss_source_id?: number
}) => {
  formData.value = {
    ...formData.value,
    name: data.title,
    rss_url: data.rss_url,
    fansub: data.fansub,
    language: data.language || '',  // 新增
    rss_source_id: data.rss_source_id,
    source_type: data.rss_source_id ? 'rss_source' : 'manual'
  }
  showRssStep.value = false
  showModal.value = true
}
```

**表单UI显示**:
```vue
<n-form-item label="字幕组">
  <n-input v-model:value="formData.fansub" placeholder="字幕组名称" />
</n-form-item>

<!-- 新增:字幕语言显示 -->
<n-form-item label="字幕语言" v-if="formData.language">
  <n-tag type="info">
    {{ formData.language === 'CHS' ? '简体中文' :
       formData.language === 'CHT' ? '繁體中文' :
       formData.language }}
  </n-tag>
</n-form-item>
```

#### 3. `web/src/api/index.ts` - API类型更新

```typescript
export interface Subscription {
  id: number
  name: string
  rss_url: string
  season: number
  status: string
  filter_keywords: string
  exclude_keywords: string
  subgroup_id?: number
  download_path: string
  rename_enabled: boolean
  last_check_time?: string
  created_at: string
  updated_at: string
  // 新增字段
  fansub?: string
  language?: string          // 新增
  update_day?: string
  total_episodes?: number
  current_episode?: number
  episode_offset?: number
  filter_rules?: string
  enabled?: boolean
  last_download_at?: string
  rss_source_id?: number
  source_type?: string
}
```

### ✅ 后端实现

#### 1. `internal/model/subscription.go` - 数据库模型更新

```go
type Subscription struct {
	ID              uint       `json:"id" gorm:"primaryKey"`
	Name            string     `json:"name" gorm:"not null;index"`
	RssURL          string     `json:"rss_url" gorm:"not null"`
	Season          int        `json:"season" gorm:"default:1"`
	Status          string     `json:"status" gorm:"default:active;index"`
	FilterKeywords  string     `json:"filter_keywords" gorm:"type:text"`
	ExcludeKeywords string     `json:"exclude_keywords" gorm:"type:text"`
	SubgroupID      *int       `json:"subgroup_id" gorm:"index"`
	DownloadPath    string     `json:"download_path"`
	RenameEnabled   bool       `json:"rename_enabled" gorm:"default:true"`
	LastCheckTime   *time.Time `json:"last_check_time"`

	// 新增字段 - 参考ani-rss
	Fansub          string     `json:"fansub" gorm:"type:varchar(100)"`
	Language        string     `json:"language" gorm:"type:varchar(10)"`    // 新增
	UpdateDay       string     `json:"update_day" gorm:"type:varchar(10)"`
	TotalEpisodes   int        `json:"total_episodes" gorm:"default:0"`
	CurrentEpisode  int        `json:"current_episode" gorm:"default:0"`
	EpisodeOffset   int        `json:"episode_offset" gorm:"default:0"`
	FilterRules     string     `json:"filter_rules" gorm:"type:text"`
	Enabled         bool       `json:"enabled" gorm:"default:true;index"`
	LastDownloadAt  *time.Time `json:"last_download_at"`

	// RSS源相关
	RSSSourceID *uint  `json:"rss_source_id" gorm:"index"`
	SourceType  string `json:"source_type" gorm:"default:manual"`

	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`

	Downloads []Download `json:"downloads,omitempty" gorm:"foreignKey:SubscriptionID"`
	RSSSource *RSSSource `json:"rss_source,omitempty" gorm:"foreignKey:RSSSourceID"`
}
```

**数据库字段**:
- `language VARCHAR(10)` - 存储语言代码 (CHS, CHT等)

## 完整用户流程

### 1. 搜索番剧

```
用户操作: 点击"搜索番剧" → 输入"葬送的芙莉莲" → 搜索
系统响应: 显示番剧列表,每个番剧显示评分、已订阅标记
```

### 2. 选择字幕组

```
用户操作: 点击展开"葬送的芙莉莲"
系统响应: 加载字幕组列表
  - 字幕组1: [Nekomoe kissaten]
    标签: 1080P, 简体, 繁体, MP4
    最新集数: 1, 2, 3
  - 字幕组2: [ANi]
    标签: 1080P, 繁体, MKV
    最新集数: 1, 2
```

### 3. 选择语言 (新功能)

```
用户操作:
  字幕组1检测到有"简体"和"繁体"标签
  系统显示语言选择:
    [简体中文] [繁體中文]

  用户点击[简体中文]按钮

系统响应:
  - 按钮变为蓝色高亮(primary type)
  - "订阅"按钮由禁用变为可点击
```

### 4. 订阅

```
用户操作: 点击"订阅"按钮
系统响应:
  1. 验证语言已选择
  2. 关闭搜索对话框
  3. 打开订阅表单,自动填充:
     - 番剧名称: 葬送的芙莉莲
     - RSS地址: https://mikanime.tv/RSS/...
     - 字幕组: Nekomoe kissaten
     - 字幕语言: 简体中文 (显示为Tag)
  4. 用户配置其他参数(更新日期、集数等)
  5. 提交创建订阅
```

### 5. 数据保存

```
数据库记录:
{
  "name": "葬送的芙莉莲",
  "rss_url": "https://mikanime.tv/RSS/...",
  "fansub": "Nekomoe kissaten",
  "language": "CHS",           ← 保存语言代码
  "update_day": "5",
  "season": 1,
  "total_episodes": 28,
  "enabled": true
}
```

## 技术亮点

### 1. 智能语言检测

从字幕组的tags数组中自动检测支持的语言:

```typescript
// 支持多种标签格式
'简体' | '简中' | 'CHS'  → 简体中文 (CHS)
'繁体' | '繁中' | 'CHT'  → 繁體中文 (CHT)
```

### 2. 条件渲染

只在字幕组支持多语言时显示语言选择:

```vue
<div v-if="getLanguageOptions(group.tags).length > 0">
  <!-- 语言选择UI -->
</div>
```

### 3. 状态管理

使用复合key管理每个字幕组的语言选择状态:

```typescript
key = `${animeUrl}_${groupRss}`
selectedLanguage.value[key] = 'CHS'
```

### 4. 表单验证

订阅前验证必选字段:

```typescript
if (languageOptions.length > 0 && !language) {
  message.warning('请先选择字幕语言')
  return
}
```

### 5. UI反馈

- 未选择语言:订阅按钮禁用(灰色)
- 已选择语言:按钮高亮,订阅按钮可点击
- 订阅成功:显示成功提示,关闭对话框

## 数据流图

```
┌─────────────────────────────────────────────────────────────┐
│ 1. 用户搜索 "葬送的芙莉莲"                                    │
└────────────────────────────┬────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. Mikan API 返回番剧列表                                     │
│    - 番剧基本信息(标题、封面、URL)                             │
└────────────────────────────┬────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. 用户展开番剧 → 加载字幕组列表                               │
│    GET /api/v1/mikan/fansub-groups?url=...                  │
└────────────────────────────┬────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. 返回字幕组数据:                                            │
│    {                                                        │
│      name: "Nekomoe kissaten",                             │
│      rss: "https://mikanime.tv/RSS/...",                   │
│      tags: ["1080P", "简体", "繁体", "MP4"],                │
│      episodes: ["1", "2", "3"]                             │
│    }                                                       │
└────────────────────────────┬────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. 前端提取语言选项:                                          │
│    getLanguageOptions(tags) → [                            │
│      { label: '简体中文', value: 'CHS' },                  │
│      { label: '繁體中文', value: 'CHT' }                   │
│    ]                                                       │
└────────────────────────────┬────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ 6. 用户点击语言按钮 → 状态更新:                                │
│    selectedLanguage[key] = 'CHS'                           │
│    订阅按钮可用                                              │
└────────────────────────────┬────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ 7. 用户点击订阅 → emit事件:                                   │
│    {                                                       │
│      title: "葬送的芙莉莲",                                  │
│      rss_url: "https://...",                               │
│      fansub: "Nekomoe kissaten",                           │
│      language: "CHS"                                       │
│    }                                                       │
└────────────────────────────┬────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ 8. 订阅表单填充并显示:                                         │
│    formData.language = 'CHS'                               │
│    显示: [简体中文] Tag                                       │
└────────────────────────────┬────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ 9. 提交创建订阅:                                              │
│    POST /api/v1/subscriptions                              │
│    { ..., language: 'CHS', ... }                           │
└────────────────────────────┬────────────────────────────────┘
                             ↓
┌─────────────────────────────────────────────────────────────┐
│ 10. 数据库保存:                                               │
│     subscriptions.language = 'CHS'                         │
└─────────────────────────────────────────────────────────────┘
```

## 构建结果

### 前端
```bash
✓ 3656 modules transformed
dist/assets/index-B68tmqvs.css    0.58 kB │ gzip: 0.39 kB
dist/assets/index-CkZboqwZ.js   828.70 kB │ gzip: 241.08 kB
✓ built in 2.88s
```

**增量**: +1.42KB (相比上一版 827.28KB)

### 后端
```bash
go build -o auto-rss cmd/server/main.go
✓ 编译成功
```

**新增字段**: `language VARCHAR(10)`

## 功能对比

### 实现前 vs 实现后

| 功能 | 实现前 | 实现后 | 改进 |
|------|--------|--------|------|
| 搜索番剧 | ✅ | ✅ | - |
| 显示字幕组 | ✅ | ✅ | - |
| 显示标签 | ✅ | ✅ | - |
| 选择语言 | ❌ | ✅ | **新增** |
| 语言验证 | ❌ | ✅ | **新增** |
| 保存语言偏好 | ❌ | ✅ | **新增** |
| 语言显示 | ❌ | ✅ | **新增** |

## 用户体验优化

### 1. 智能显示
- 只有多语言字幕组才显示语言选择
- 单语言字幕组直接订阅,无需选择

### 2. 视觉反馈
- 未选择:按钮默认样式,订阅禁用
- 已选择:按钮高亮蓝色,订阅可用
- 订阅表单:Tag形式显示已选语言

### 3. 错误预防
- 订阅前验证语言已选择
- 友好的错误提示:"请先选择字幕语言"

### 4. 状态持久化
- 选择的语言保存到数据库
- 编辑订阅时显示当前语言

## 待实现功能

### 高优先级

1. **数据库迁移**
   ```bash
   # 需要运行迁移添加language字段
   ALTER TABLE subscriptions ADD COLUMN language VARCHAR(10);
   ```

2. **语言过滤下载**
   - 根据选择的语言过滤RSS feed
   - 只下载匹配语言的剧集

### 中优先级

3. **多语言扩展**
   - 支持日语原版(JPN)
   - 支持英文字幕(ENG)
   - 支持韩语字幕(KOR)

4. **语言偏好设置**
   - 用户可设置默认语言偏好
   - 新订阅自动选择默认语言

### 低优先级

5. **语言统计**
   - 显示每种语言的订阅数量
   - 按语言分组显示订阅

6. **语言切换**
   - 允许修改现有订阅的语言
   - 重新获取对应语言的剧集

## 测试建议

### 1. UI测试

```bash
# 启动服务
./auto-rss

# 访问 http://localhost:8080
1. 点击"搜索番剧"
2. 搜索"葬送的芙莉莲"
3. 展开番剧查看字幕组
4. 验证语言选择按钮显示
5. 点击选择语言
6. 验证按钮高亮
7. 点击订阅
8. 验证表单预填充
9. 提交创建订阅
10. 检查数据库language字段
```

### 2. 边界测试

| 测试场景 | 预期结果 |
|---------|---------|
| 字幕组无语言标签 | 不显示语言选择,直接订阅 |
| 字幕组只有一种语言 | 不显示语言选择,自动使用该语言 |
| 字幕组有简繁两种语言 | 显示两个按钮,必须选择 |
| 未选择语言点击订阅 | 提示"请先选择字幕语言" |
| 选择语言后点击订阅 | 成功,表单显示选择的语言 |

### 3. 数据验证

```sql
-- 检查订阅记录
SELECT name, fansub, language, enabled
FROM subscriptions
WHERE name LIKE '%葬送%';

-- 预期结果:
-- name: 葬送的芙莉莲
-- fansub: Nekomoe kissaten
-- language: CHS
-- enabled: 1
```

## 总结

✅ **完整实现了字幕语言选择功能**:
- 前端: 语言检测 + 选择UI + 状态管理 + 表单集成
- 后端: 数据库字段扩展 + API支持
- 用户体验: 智能显示 + 视觉反馈 + 错误预防

✅ **用户流程优化**:
1. 搜索番剧 ✓
2. 查看字幕组 ✓
3. **选择语言** ✓ (新增)
4. 订阅并保存语言偏好 ✓

✅ **技术实现**:
- 响应式状态管理
- 条件渲染优化
- 表单验证增强
- 数据模型扩展

用户现在可以精确控制订阅的字幕语言,避免下载不需要的语言版本!
