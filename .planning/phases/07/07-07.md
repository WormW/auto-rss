---
name: Integration Tests and API Validation
description: Integration tests for all new APIs
type: test
---

# Plan 07-07: 集成测试和 API 验证

## Status

Completed. Phase 7 handler/API test work was published through PR #38/#39/#40, and router-level/API validation was completed through PR #43/#44/#45/#46. PR #46 (`WOR-174: add router-level API integration tests`) was the final published slice and merged on 2026-06-28.

## Goal
执行端到端集成测试，验证所有新增 API 正常工作。

## Files

### Create/Modify
- `internal/api/router/router_test.go` (新增和扩展 router.Setup 级测试用例)

## Integration Test Cases

### 1. 标签系统端到端测试
- [x] 创建标签 → 查询列表 → 更新 → 删除
- [x] 为订阅添加标签 → 查询订阅标签 → 移除标签
- [x] 路由边界验证无效 ID、重复标签、空 tag_ids、缺失订阅等错误场景

### 2. 下载历史端到端测试
- [x] 创建下载记录 → 查询历史
- [x] 使用订阅、状态、日期筛选条件
- [x] 分页查询

### 3. 下载统计端到端测试
- [x] 验证统计数据准确性
- [x] 验证 `days` 参数经由路由到达 handler

### 4. RSS健康检查端到端测试
- [x] 检查单个订阅
- [x] 检查所有订阅
- [x] 获取失效订阅
- [x] 手动触发 RSS 健康检查任务

## API 文档验证
- [x] 检查路由注册正确
- [x] 检查响应格式统一
- [x] 检查错误码一致
- [x] 检查鉴权启用时 Phase 7 路由返回 401

## Manual Verification (可选)
- [ ] 使用 curl/Postman 测试关键 API
- [ ] 验证响应数据结构

## Success Criteria
- [x] 所有 router/API 集成测试通过
- [x] API 与文档一致
- [x] 未发现阻塞问题

## Published Evidence

- PR #43 (`WOR-159`) added router-boundary coverage for Phase 7 tag, download history/statistics, and RSS health API paths through `router.Setup`.
- PR #44 (`WOR-164`) expanded route-level tag API validation through the real `/api/v1` routes.
- PR #45 (`WOR-169`) confirmed Phase 7 routes are protected when authentication is enabled.
- PR #46 (`WOR-174`) added final router-level download history filter/pagination coverage plus RSS health aggregate, dead-list, and async trigger coverage.
